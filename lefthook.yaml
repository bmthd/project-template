pre-push:
  parallel: true
  commands:
    type-check:
      run: bun type:check
    spell-check:
      run: bun spell:check
pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
      run: bun format:fix
      stage_fixed: true